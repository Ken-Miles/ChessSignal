name: chesssignal

services:
  app:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    depends_on:
      - database
  database:
    image: mongo:latest
    volumes:
      - chesssignal-database:/data/db
    attach: false

volumes:
  chesssignal-database: